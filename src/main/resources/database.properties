# Cấu hình kết nối cơ sở dữ liệu MySQL XAMPP
db.url=jdbc:mysql://localhost:3306/bookstore?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=utf8&useUnicode=true
db.username=root
db.password=

# Cấu hình kết nối pool (tùy chọn)
db.maxConnections=10
db.connectionTimeout=30000
